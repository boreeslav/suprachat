# План: шифрование для ботов и пер-групповой переключатель шифрования

> Статус: **черновик плана, ожидает акцепта**. Реализация ведётся по частям, каждая часть принимается отдельно.

## 1. Цель

1. Дать боту **два режима общения**, работающих параллельно:
   - **Открытый** (как сейчас) — plaintext по REST `/api/bot-api/*` и WS `/ws/bot`.
   - **Приватный** — текстовые сообщения шифруются так же, как у пользователей (`E1:` + AES-GCM, RSA-OAEP для обмена ключами).
2. **Медиа (файлы/вложения) и mini-app канал остаются по открытому протоколу** — не шифруются (осознанное ограничение, как и для пользователей сейчас).
3. **Вернуть группам приватность**: добавить **переключатель шифрования в самой группе** (per-chat), который активирует протокол шифрования для этой группы.
4. Бот **может** использовать шифрование в личных чатах; в **зашифрованной группе обязан** его использовать.
5. **Цель безопасности:** сервер не хранит переписку в открытом виде. Пользователи — и допущенные в группы боты — держат свои пароли/ключи **под собственной ответственностью**.

## 2. Текущее состояние (точки интеграции)

### 2.1 Криптосхема (есть, только для людей)
- Мастер-пароль → PBKDF2 (310k) → мастер-ключ. RSA-пара **детерминированно** выводится из мастер-пароля + salt + `userId` (`supra-crypto.js`, `supra-webcrypto.bundle.js`).
- Личные чаты: ключ = HKDF от мастер-ключа и пары `userId`.
- Группы: автопароль, обёрнутый RSA-OAEP под публичный ключ каждого участника, хранится в `data/chat-member-keys.json` (`SupraChatMemberKeyRecord`). Доп. пароль группы (tier `protected`) — опционально.
- Шифротекст помечается префиксом `E1:`; сервер — zero-knowledge relay (`SupraMessengerService.IsEncryptedPayload`).
- Эндпоинты: `Controllers/EncryptionController.cs` (`status`, `setup`, `private-key-backup`, `public-keys`, `group-keys`, `reset`) — все `[Authorize]` (cookie).

### 2.2 Групповое шифрование = ГЛОБАЛЬНЫЙ флаг
- `AppAppearanceSettings.EnableGroupEncryption` (`Core/Entities/AppAppearanceSettings.cs`), задаётся в `AdminController`, отдаётся клиенту через `AppAppearanceService` (`enableGroupEncryption`).
- Клиент: `MessengerThemeManager.#adminEnableGroupEncryption` / `isAdminGroupEncryptionEnabled()`. От него зависит вся групповая крипта в `supra-messenger.js` (`#isPlaintextChatId`, `#ensureGroupChatEncryptionKeys`, `#setupGroupKeys`, распространение ключей).
- **Нет** пер-группового переключателя.

### 2.3 Боты = всегда plaintext
- Клиент: `#isBotChatId(chatId)` → `#isPlaintextChatId` = true (чат бота никогда не шифруется).
- Сервер `Infrastructure/Services/BotApiService.cs`:
  - `sendMessage`: отклоняет `E1:` («Боты отправляют только незашифрованные сообщения»).
  - `editMessage`: отклоняет `E1:`.
  - `RecordIncomingMessageAsync`: пропускает (не кладёт в инбокс) сообщения `IsEncryptedPayload`.
  - Сообщения бота сохраняются с `encryptionTier: "basic"` (plaintext).
- Транспорт бота: приём — WS `/ws/bot`; отправка — REST `/api/bot-api/*`; аутентификация `login`+`token` (SHA-256 hash, `BotApiService.AuthenticateAsync`).
- У бот-пользователя не заполнены `EncryptionPublicKey` / `EncryptionPrivateKeyBlob`.

### 2.4 Хранилище
- `FileDataStore` (JSON-файлы в `data/`). Сообщения — `SupraChatMessageRecord.Text` (plaintext или `E1:`), поле `EncryptionTier`. Инбокс бота — `BotInboxMessageRecord` (хранится 1 сутки). Файлы — `uploads/` (не шифруются).

### 2.5 CursorBot (клиент бота)
- `src/supra-bot-api.ts` — единственный сетевой клиент (WS + REST). `src/message-handler.ts` — логика. `src/config.ts` — env (`SUPRA_BASE_URL`, `SUPRA_BOT_LOGIN`, `SUPRA_BOT_TOKEN`). Node 20+ (есть `crypto.subtle`).

## 3. Целевая архитектура

### 3.1 Хранение ключей бота (под ответственностью владельца)
- Новый env-секрет `SUPRA_BOT_MASTER_PASSWORD` (в `CursorBot/.env`, уже в `.gitignore`). **Опционален**: если не задан — бот работает в открытом режиме (см. ниже про поддержку шифрования).
- Бот при старте **детерминированно** выводит RSA-пару из мастер-пароля + salt + своего `botUserId` (тот же алгоритм, что у веб-клиента) и публикует на сервере: `EncryptionSalt`, `EncryptionVerifier`, `EncryptionPublicKey`, `EncryptionPrivateKeyBlob` через бот-аутентифицированный аналог `/api/encryption/setup`.
- Приватный ключ держится только у бота (выводится из пароля), сервер хранит лишь публичный ключ + клиент-зашифрованный blob — как у людей.

### 3.1a Поддержка шифрования ботом (capability)
- **Признак поддержки** = у бот-пользователя на сервере опубликован `EncryptionPublicKey` (т.е. бот прошёл `encryptionSetup`). Отдельный флаг в `BotRecord` не нужен — источник истины один.
- **Бот без поддержки**: может отправлять сообщения (в т.ч. в зашифрованные группы — в открытом виде), но **не может читать** зашифрованные сообщения групп (они не попадут в его инбокс как читаемые). Это допустимый режим.
- **Личный чат с ботом**: клиент шифрует, только если бот поддерживает шифрование (есть публичный ключ). В UI чат с поддерживающим шифрование ботом помечается как **защищённый** (иконка-щит рядом с именем / пометка в профиле — **не** замочек). Контакт/чат-DTO отдаёт `botSupportsEncryption`.

### 3.2 Два протокола параллельно
| Канал | Режим | Поведение |
|-------|-------|-----------|
| Личный чат бота | открытый (по умолчанию) или приватный | бот **может** шифровать текст |
| Группа без переключателя | открытый | как сейчас |
| Группа с переключателем шифрования | приватный | бот **обязан** шифровать текст |
| Медиа/файлы | открытый | всегда plaintext (вложения) |
| Mini-app канал (`/ws/mini-app`) | открытый | всегда plaintext |
| Каналы (channel) | открытый | как сейчас |

### 3.3 Пер-групповой переключатель
- Новое поле `SupraChatRecord.EncryptionEnabled` (bool, по умолчанию false → сохраняет текущее поведение групп).
- Переключатель в настройках группы (только админ). При включении: распространяются ключи всем участникам, **включая ботов** (RSA-OAEP врап автопароля под публичный ключ бота); новые сообщения требуют `E1:`.
- Семантика **глобального** флага `EnableGroupEncryption` (решение): это **разрешение включать** шифрование групп, а не принудительный режим.
  - Глобально **OFF** → нельзя **включать** шифрование в новых группах; уже зашифрованные группы остаются зашифрованными (глобальная настройка их **не отключает**).
  - Глобально **ON** → админ группы может включить пер-групповой переключатель.
  - На текущем этапе глобальный флаг **выключаем**, пер-групповая опция — по умолчанию выключена.
- Клиентский гейт `isAdminGroupEncryptionEnabled()` остаётся как «разрешено включать»; фактическое шифрование чата определяется пер-чатовым `chat.encryptionEnabled`.

## 4. Разбивка на части (этапы с раздельным акцептом)

> Каждый этап: код → сборка/линт → краткий отчёт → **акцепт** → коммит этапа.

### Этап 0 — База и план (текущий)
- Сохранить этот план в `docs/`.
- Коммит + пуш текущего рабочего состояния и плана (без личных данных/секретов; `.env`, `bot-config.json`, `data/`, deploy-секреты уже в `.gitignore`).

### Этап 1 — Криптомодуль и ключи бота (сервер + CursorBot) — ✅ ВЫПОЛНЕНО
- Сервер: бот-аутентифицированные методы `/api/bot-api/encryptionStatus` и `/encryptionSetup` поверх `SupraEncryptionService` (новый partial `BotApiService.Encryption.cs`, DTO в `BotApiDtos.cs`, эндпоинты в `BotApiController`). Cookie-`EncryptionController` не тронут.
- CursorBot: env `SUPRA_BOT_MASTER_PASSWORD`; модуль `src/crypto/supra-bot-crypto.ts` (PBKDF2 310k, детерминированная RSA-2048 через node-forge с seeded PRNG, AES-GCM `E1:`, RSA-OAEP unwrap, deriveChatKey) и `src/crypto/bot-encryption.ts` (инициализация/публикация при старте).
- На старте бот публикует свой публичный ключ (если задан мастер-пароль); иначе — открытый режим.
- **Поведение сообщений не изменено** (только инфраструктура ключей).
- **Проверка совместимости пройдена**: self-test forge (веб-клиент) ↔ native WebCrypto (бот) — детерминизм, RSA-OAEP unwrap, AES-GCM `E1:` в обе стороны — все OK.

### Этап 2 — Приватные личные чаты бота — ✅ ВЫПОЛНЕНО
- **Уточнение по схеме:** личные чаты используют тот же механизм, что и группы (рандомный автопароль, обёрнутый RSA-OAEP под публичный ключ каждого участника, AES-ключ = `PBKDF2(autoPassword, "group-auto|"+chatId)`), а не отдельный HKDF. Бот использует те же примитивы Этапа 1 (`unwrapAutoPassword` + `deriveChatKey`).
- **Сервер:**
  - `SupraMessengerService.Messages.cs`: гейт личных чатов с ботом ослаблен — `E1:` (tier `basic`) разрешён, если бот поддерживает шифрование (опубликован публичный ключ); tier `protected` для ботов отклоняется (бот не владеет доп. паролем). `isPlaintextChat` для бот-чата с шифрованием = false.
  - `SupraMessengerService.Bots.cs`: новый `DirectBotChatSupportsEncryptionAsync`.
  - `BotApiService`: `sendMessage`/`editMessage` разрешают `E1:` в личных чатах; `RecordIncomingMessageAsync` доставляет `E1:` в инбокс/WS бота только в личном чате и только E2EE-боту; новый бот-метод `getGroupKey` (обёрнутый автопароль) + эндпоинт.
  - `SupraChatDto.botSupportsEncryption` (+ в `CreateDirectChat`-ответе) — клиент понимает, что чат с ботом можно шифровать.
- **Веб-клиент:** `#isPlaintextChatId` для бот-чата = `!botSupportsEncryption`; флаг проброшен в `chatsMeta` и в оптимистично созданный чат. Шифрование/раздача ключей идут штатным механизмом личных чатов.
- **CursorBot:** прозрачное шифрование в `SupraBotApi` — `attachEncryption(crypto)`, кэш ключей чатов (через `getGroupKey` → `unwrapAutoPassword` → `deriveChatKey`), шифрование исходящего текста (`sendMessage`/`editMessage`, в т.ч. ответы по `userLogin` через `encryptChatId`) и расшифровка входящего текста/превью (WS + догрузка). Медиа/кнопки/подписи/mini-app — без шифрования.
- Открытый режим сохраняется: без ключа чата (или у бота без мастер-пароля) текст идёт plaintext.
- **Проверка:** сборка сервера и бота — OK; self-test координатора бота ↔ forge (веб): бот→веб шифрование, веб→бот расшифровка, открытый чат без ключа, кэш ключа — все OK.
- **Известное ограничение:** ответы режима «помощник» (`assistantReply`, по `sessionId`) пока plaintext; ответы основного диалога (`sendMessage`) шифруются.

### Этап 3 — Пер-групповой переключатель (сервер + модель) — ✅ ВЫПОЛНЕНО
- **Модель:** `SupraChatRecord.EncryptionEnabled` (bool, дефолт false). Миграция не нужна — `System.Text.Json` подставляет `false` для старых групп в `chats.json`.
- **API мессенджера:** новое действие `SetGroupEncryption` (`SupraMessengerController` → `SupraMessengerService.SetGroupEncryptionAsync`). Только админ группы; **включение требует глобального разрешения** `EnableGroupEncryption` (читается через `AppAppearanceService.GetEffectiveAsync`), выключение разрешено всегда. После изменения — broadcast `SupraGroupUpdated`. Флаг отдаётся в `SupraChatDto`, `SupraGetGroupInfoResponse`, `SupraWsGroupUpdatedPayload`.
- **Бот-API:** `BotApiGetChatInfoResponse.encryptionEnabled` (заполняется в `GetChatInfoAsync` для групп). Входящие сообщения помечаются `encryptionEnabled` (`BotInboxMessageRecord.EncryptionEnabled` → `BotApiMessageDto.encryptionEnabled` в `getMessages` и WS). Фактическая доставка `E1:` групповых сообщений боту — этап 4 (пока зашифрованные групповые сообщения в инбокс бота не кладутся).
- **Гейт отправки:** в зашифрованной группе текстовые сообщения обязаны быть `E1:` (`SupraMessengerService.Messages.cs`). Исключения: боты без поддержки шифрования (могут слать plaintext — решение §6.2; полноценное групповое шифрование бота — этап 4) и медиа без текста (вложения остаются открытыми).
- **Веб-клиент:** `encryptionEnabled` проброшен в `#mapChatDto` → `#chatsMeta`, в `updateChatMeta` и обработчик `SupraGroupUpdated`. Привязка `#isPlaintextChatId`/`#shouldEncryptChat` к пер-чатовому флагу и UI-переключатель — этап 5.
- **Проверка:** сборка сервера и бота — OK, новых предупреждений/линта нет.

### Этап 4 — Бот в зашифрованной группе (обязательное шифрование) — ✅ ВЫПОЛНЕНО
- **Сервер:**
  - `BotApiService.RecordIncomingMessageAsync`: `E1:` групповые сообщения доставляются боту в инбокс/WS, если бот поддерживает шифрование (опубликован публичный ключ) **и** у него есть собственный групповой ключ (`SupraChatMemberKeyRecord` для этого чата) — тогда он способен расшифровать. В личном чате — по-прежнему достаточно публичного ключа (Этап 2).
  - `BotApiService.SendMessageAsync`/`EditMessageAsync`: `E1:` от бота разрешён не только в личных чатах, но и в зашифрованных группах (`SupraChatRecord.EncryptionEnabled`). Релакс guard'а в `SupraMessengerService.Messages.cs` для ботов уже был (бот без ключа может слать plaintext как fallback — решение §6.2).
  - Бот-аутентифицированный `getGroupKey` (Этап 1/2) уже работает для любого `chatId`, включая группы — отдельных правок не потребовалось. Сам врап автопароля под публичный ключ бота-участника выполняет админ при раздаче ключей (веб-клиент, Этап 5).
- **CursorBot:** изменений не потребовалось — прозрачное шифрование в `SupraBotApi` (`_chatKey`/`_encryptText`/`_decryptText` через `getGroupKey` → `unwrapAutoPassword` → `deriveChatKey`) **chat-agnostic**: для групп `replyTarget` отдаёт `{ chatId }`, поэтому исходящий групповой текст шифруется, а входящий `E1:` расшифровывается тем же механизмом, что и в личных чатах. Без группового ключа — fallback в plaintext (открытый режим).
- **Проверка:** сборка сервера (`dotnet build`) и бота (`tsc --noEmit`) — OK; новых предупреждений/линта нет (4 предупреждения предсуществующие).
- **Известное ограничение:** обязательность шифрования бота в группе на сервере не форсируется (бот без ключа шлёт plaintext); фактическое шифрование обеспечивает наличие группового ключа у бота. Раздача ключа боту при включении шифрования группы — Этап 5 (веб-UI).

### Этап 5 — Веб-UI пер-группового переключателя
- Переключатель «Шифрование группы» в профиле/настройках группы (админ).
- При включении: распространить ключи всем участникам, **включая бота**; предупредить, если у участника/бота нет опубликованного ключа.
- Привязать `#isPlaintextChatId`, `#shouldEncryptChat`, `#ensureGroupChatEncryptionKeys` к пер-чатовому флагу.

### Этап 6 — Документация, миграция, проверки
- Обновить `05-encryption.md` (бот как участник E2EE, пер-групповой переключатель) и `07-bot-api.md` (приватный режим, обязательность в зашифрованных группах, что медиа/mini-app остаются открытыми).
- Сценарии проверки: личный приватный чат, включение шифрования группы с ботом, миграция старых групп, fallback на открытый режим.

## 5. Влияние и риски
- Бот всё равно расшифровывает у себя → содержимое попадает в процесс бота/агента. Шифрование добавляет бота в круг доверия, но не скрывает переписку **от самого бота**.
- Управление мастер-паролем бота — новый класс секретов (env). Утечка = доступ к переписке бота.
- Инбокс: для зашифрованного пути сервер хранит `E1:`; догрузка `getMessages` отдаёт шифротекст, бот расшифровывает.
- Совместимость: дефолт `EncryptionEnabled=false` сохраняет текущее поведение существующих групп.
- Медиа и mini-app остаются открытыми — это явно зафиксированная граница, не дыра «по недосмотру».

## 6. Принятые решения
1. **Глобальный флаг `EnableGroupEncryption`** — это «разрешение включать» групповое шифрование. Глобальный OFF не отключает уже зашифрованные группы, лишь запрещает включать новые. На текущем этапе глобально выключен; пер-групповая опция по умолчанию выключена.
2. **Личные чаты бота**: шифруются, если бот **поддерживает** шифрование (опубликован публичный ключ). Бот без поддержки может слать plaintext (в т.ч. в зашифрованные группы) и просто не читает зашифрованные групповые сообщения. UI помечает чат с поддерживающим ботом как «защищённый» (щит/пометка в профиле).
3. **Хранение мастер-пароля бота**: env (`SUPRA_BOT_MASTER_PASSWORD`); деталь реализации бота, главное — работоспособность.
