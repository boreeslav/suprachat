# Changelog items before deploy (one per line). Lines starting with # are ignored.

Синхронизация RequestSync: один запрос при загрузке и переподключении вместо множества GetChats/GetMessages.
Меньше лишних запросов при переключении чатов — профили и список из бандла и кеша.
Исправлено прочтение сообщений при открытом чате и исчезновение ложного badge после возврата в приложение.
Ускорена сборка списка чатов на сервере (одно чтение данных вместо повторных проходов).
