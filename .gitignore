# Local scratch, deploy secrets, build artifacts (not for repo)
tmp/

# Cursor agent config (local only — не коммитить, логика работает из локальных файлов)
.cursor/
AGENTS.md

# Generated at deploy time (written into tmp build tree; ignore if created locally)
src/SuperMessenger.Web/deploy-branding.json

# .NET build output
[Bb]in/
[Oo]bj/

# Node
node_modules/
**/node_modules/

# Client minified bundles (build-client-assets.ps1 / deploy)
src/SuperMessenger.Web/wwwroot/messenger/*.min.js

# CursorBot local runtime (sources are tracked under CursorBot/)
CursorBot/.env
CursorBot/bot-config.json
CursorBot/actions.json
CursorBot/scripts/actions/
CursorBot/tmp-*
CursorBot/dist/
CursorBot/dist.backup.*/

# Runtime app data (users, keys, uploads)
**/data/

# IDE
.vs/
*.user
*.suo
*.DotSettings.user

# OS
Thumbs.db
.DS_Store

# Local deploy secrets and nginx site config (examples are tracked)
deploy/nginx/supermessenger.conf
